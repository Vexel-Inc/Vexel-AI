let title_Header = document.querySelector('.title');

// Document Body Function
// document.addEventListener('contextmenu', (e) => {
//    e.preventDefault();
// });

// Titile Class On Window Loaded
window.onload = () => {
   title_Header.classList.add('position_move_to_left');
}
